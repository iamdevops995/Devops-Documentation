# PowerShell

Scripting and automation notes for Windows and cross-platform PowerShell.

> Sections below match the sidebar. Content is being filled in incrementally.

## Basics

### Cmdlets

Verb-noun naming, discovery with `Get-Command` and `Get-Help`, common parameters.

### Pipeline

Object flow, `ForEach-Object`, `Where-Object`, `Select-Object`, formatting.

### Variables and Types

Scopes, type casting, arrays, hashtables, `PSCustomObject`.

## Scripting

### Functions

Parameters, validation attributes, `begin`/`process`/`end`, advanced functions.

### Modules

Manifests, exporting members, module path, publishing.

### Error Handling

Terminating vs non-terminating errors, `try`/`catch`/`finally`,
`$ErrorActionPreference`.

## Remoting

WinRM and SSH transports, sessions, `Invoke-Command`, credential handling.
