# Linux

Administration notes, command references and diagnostics.

> Sections below match the sidebar. Content is being filled in incrementally.

## Filesystem

Layout, mounts and `fstab`, LVM, disk usage, inode exhaustion.

## Processes and systemd

Unit files, targets, `systemctl` and `journalctl`, service dependencies, timers.

## Users and Permissions

Users and groups, `sudoers`, file modes, ACLs, SUID and sticky bits.

## Networking

Interfaces and addressing, routing, `ss` and `ip`, firewalls, DNS resolution.

## Performance

CPU, memory, disk and network diagnostics; load average, `top`, `vmstat`,
`iostat`, `pidstat`.
