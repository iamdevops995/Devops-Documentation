# Ansible

Configuration management notes and playbook patterns.

> Sections below match the sidebar. Content is being filled in incrementally.

## Inventory

Static and dynamic inventories, groups, host variables, `group_vars` layout.

## Playbooks

### Tasks

Modules, loops, conditionals, `changed_when`, idempotency.

### Handlers

Notify, listen, flush handlers, restart patterns.

### Templates

Jinja2 syntax, filters, `template` module, validation.

## Roles

Directory layout, defaults vs vars, dependencies, Galaxy requirements.

## Vault

Encrypting variables and files, vault IDs, integration with CI pipelines.
