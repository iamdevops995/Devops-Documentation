# Jenkins

CI/CD notes, pipeline patterns and agent configuration.

> Sections below match the sidebar. Content is being filled in incrementally.

## Pipelines

### Declarative Pipeline

Stages, steps, `when` conditions, `post` blocks, parallel stages, credentials.

### Scripted Pipeline

Groovy structure, `node` blocks, try/catch handling, when to prefer it.

### Shared Libraries

Repository layout, `vars` and `src`, loading, versioning, testing.

## Agents

Static and ephemeral agents, labels, Docker and Kubernetes agents, tool installs.

## Installation

Controller installation, initial setup, plugin management, backups.

See also `Install Jenkins.txt` at the repository root.
