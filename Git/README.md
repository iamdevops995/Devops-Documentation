# Git

Everyday commands, branching models and recovery notes.

> Sections below match the sidebar. Content is being filled in incrementally.

## Branching

Trunk based vs GitFlow, naming conventions, short-lived branches, protection
rules.

## Merging and Rebasing

Fast-forward vs merge commits, interactive rebase, conflict resolution, when to
use which.

## Undo and Recovery

`reset` vs `revert` vs `restore`, recovering commits with `reflog`, removing
files from history.

## Hooks

Client-side and server-side hooks, pre-commit checks, secret scanning
integration.
